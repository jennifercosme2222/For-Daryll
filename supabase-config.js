// Fill these in with your own Supabase project values
// (Project Settings → API → Project URL / anon public key).
// This is optional — the site works perfectly without it.
// If left as-is, the site just skips the "open" logging feature.

const SUPABASE_URL = 'YOUR-PROJECT.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
